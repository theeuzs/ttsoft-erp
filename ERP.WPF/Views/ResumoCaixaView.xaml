<Window x:Class="ERP.WPF.Views.ResumoCaixaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resumo Financeiro" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen" WindowStyle="None" 
        AllowsTransparency="True" Background="Transparent">
    
    <Border Background="{DynamicResource FundoRodape}" CornerRadius="16" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Opacity="0.3" ShadowDepth="5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource CardBg}" CornerRadius="16,16,0,0" Padding="20" BorderBrush="{DynamicResource BordaCard}" BorderThickness="0,0,0,1">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" FontSize="22" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding NumeroCaixa, StringFormat='Resumo Financeiro do Caixa {0}'}" FontSize="22" FontWeight="Bold" Foreground="#1E62A6" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" Foreground="{DynamicResource TextMuted}" FontSize="20" BorderThickness="0" Cursor="Hand" Click="Fechar_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    
                    <Border Background="{DynamicResource CardBg}" CornerRadius="8" Padding="20" Margin="0,0,0,15" BorderBrush="{DynamicResource BordaCard}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Movimento Geral" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,15"/>
                            <Grid Margin="0,10,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Dinheiro" HorizontalAlignment="Left" Foreground="Gray" Margin="0,2"/>
                                <TextBlock Grid.Row="0" Text="{Binding VendasDinheiro, StringFormat=C2}" HorizontalAlignment="Right" Foreground="#0A3D62" FontWeight="SemiBold" Margin="0,2"/>

                                <TextBlock Grid.Row="1" Text="Pix" HorizontalAlignment="Left" Foreground="Gray" Margin="0,2"/>
                                <TextBlock Grid.Row="1" Text="{Binding VendasPix, StringFormat=C2}" HorizontalAlignment="Right" Foreground="#0A3D62" FontWeight="SemiBold" Margin="0,2"/>

                                <TextBlock Grid.Row="2" Text="Cartão de Débito" HorizontalAlignment="Left" Foreground="Gray" Margin="0,2"/>
                                <TextBlock Grid.Row="2" Text="{Binding VendasCartaoDebito, StringFormat=C2}" HorizontalAlignment="Right" Foreground="#0A3D62" FontWeight="SemiBold" Margin="0,2"/>

                                <TextBlock Grid.Row="3" Text="Cartão de Crédito" HorizontalAlignment="Left" Foreground="Gray" Margin="0,2"/>
                                <TextBlock Grid.Row="3" Text="{Binding VendasCartaoCredito, StringFormat=C2}" HorizontalAlignment="Right" Foreground="#0A3D62" FontWeight="SemiBold" Margin="0,2"/>

                                <TextBlock Grid.Row="4" Text="A Prazo" HorizontalAlignment="Left" Foreground="Gray" Margin="0,2"/>
                                <TextBlock Grid.Row="4" Text="{Binding VendasAPrazo, StringFormat=C2}" HorizontalAlignment="Right" Foreground="#0A3D62" FontWeight="SemiBold" Margin="0,2"/>

                                <TextBlock Grid.Row="5" Text="Haver" HorizontalAlignment="Left" Foreground="Gray" Margin="0,2"/>
                                <TextBlock Grid.Row="5" Text="{Binding VendasHaver, StringFormat=C2}" HorizontalAlignment="Right" Foreground="#0A3D62" FontWeight="SemiBold" Margin="0,2"/>
                            </Grid>
                            <Separator Margin="0,0,0,10"/>
                            <Grid> 
                                <TextBlock Text="Total Movimentado" FontWeight="Black" Foreground="#0F172A"/> 
                                <TextBlock Text="{Binding TotalMovimentado, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" FontWeight="Black" FontSize="16" Foreground="#0F172A"/> 
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Background="#FEFCE8" CornerRadius="8" Padding="20" BorderBrush="#FEF08A" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Dinheiro na Gaveta" FontWeight="Bold" Foreground="#854D0E" Margin="0,0,0,15"/>
                            <Grid Margin="0,0,0,5"> 
                                <TextBlock Text="+ Saldo Inicial" Foreground="#A16207"/> 
                                <TextBlock Text="{Binding SaldoInicial, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#0F172A"/>
                            </Grid>
                            <Grid Margin="0,0,0,5"> 
                                <TextBlock Text="+ Vendas (Dinheiro)" Foreground="#A16207"/> 
                                <TextBlock Text="{Binding VendasDinheiro, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#0F172A"/>
                            </Grid>
                            <Grid Margin="0,0,0,5"> 
                                <TextBlock Text="+ Suprimentos" Foreground="#A16207"/> 
                                <TextBlock Text="{Binding Suprimentos, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#16A34A"/>
                            </Grid>
                            <Grid Margin="0,0,0,15"> 
                                <TextBlock Text="- Sangrias" Foreground="#A16207"/> 
                                <TextBlock Text="{Binding Sangrias, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="#DC2626"/>
                            </Grid>
                            <Separator Margin="0,0,0,10" Background="#FDE047"/>
                            <Grid> 
                                <TextBlock Text="EM ESPÉCIE =" FontWeight="Black" Foreground="#713F12" FontSize="16"/> 
                                <TextBlock Text="{Binding TotalEmEspecie, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" FontWeight="Black" FontSize="18" Foreground="#854D0E"/> 
                            </Grid>
                        </StackPanel>
                    </Border>

                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,20,0">
                    
                    <Border Background="#F0F9FF" CornerRadius="8" Padding="20" Margin="0,0,0,15" BorderBrush="#BAE6FD" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Operador" HorizontalAlignment="Center" FontSize="12" Foreground="#0284C7"/>
                            <TextBlock Text="{Binding OperadorNome}" HorizontalAlignment="Center" FontWeight="Black" FontSize="18" Foreground="#0369A1" Margin="0,5"/>
                            <TextBlock Text="{Binding StatusCaixaTexto, FallbackValue='Aberto: Hoje'}" HorizontalAlignment="Center" FontSize="11" Foreground="#0EA5E9"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{DynamicResource FundoRodape}" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="{DynamicResource BordaInput}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="📅 Consultar Histórico de:" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                            <DatePicker SelectedDate="{Binding DataConsulta, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                        Background="{DynamicResource CardBg}" BorderBrush="{DynamicResource BordaInput}" Padding="8" FontSize="14" Cursor="Hand"
                                        ToolTip="Escolha um dia do passado para ver como o caixa fechou."/>
                        </StackPanel>
                    </Border>

                    <StackPanel Visibility="{Binding VisibilidadeBotoesAcao, FallbackValue=Visible}">
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="+ Suprimento" Command="{Binding SuprimentoCommand}" Background="#10B981" Foreground="White" FontWeight="Bold" Height="60" BorderThickness="0" Cursor="Hand">
                                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                            </Button>
                            <Button Grid.Column="2" Content="— Sangria" Command="{Binding SangriaCommand}" Background="#EF4444" Foreground="White" FontWeight="Bold" Height="60" BorderThickness="0" Cursor="Hand">
                                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                            </Button>
                        </Grid>

                        <Button Content="🔒 ENCERRAR CAIXA" Command="{Binding EncerrarCaixaCommand}" Background="#B91C1C" Foreground="White" FontWeight="Black" FontSize="16" Height="60" BorderThickness="0" Cursor="Hand" Margin="0,0,0,10">
                            <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                        </Button>
                    </StackPanel>

                    <Button Content="📄 Exportar PDF do Caixa" Command="{Binding ExportarPdfCommand}"
                            Background="#DC2626" Foreground="White" FontWeight="Bold"
                            Height="44" BorderThickness="0" Cursor="Hand">
                        <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="6"/></Style></Button.Resources>
                    </Button>

                </StackPanel>

                <Border Grid.Column="2" Background="{DynamicResource CardBg}" CornerRadius="8" Padding="20" BorderBrush="{DynamicResource BordaCard}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <TextBlock Text="Extrato Detalhado" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
                            <Button Content="🖨️ Imprimir" HorizontalAlignment="Right" Background="{DynamicResource CardBgAlt}" Foreground="{DynamicResource TextMuted}" Padding="10,5" BorderThickness="0">
                                <Button.Resources> <Style TargetType="Border"> <Setter Property="CornerRadius" Value="4"/> </Style> </Button.Resources>
                            </Button>
                        </Grid>
                        <Separator Grid.Row="1" Margin="0,0,0,15"/>
                        <ListBox Grid.Row="2" ItemsSource="{Binding Extrato}" BorderThickness="0" Background="Transparent" Foreground="#10B981" FontWeight="SemiBold" FontFamily="Consolas" ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>