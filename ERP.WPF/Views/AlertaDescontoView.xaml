<Window x:Class="ERP.WPF.Views.AlertaDescontoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Liberação de Gerente" Height="380" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">
    
    <Border Background="{DynamicResource CardBg}" CornerRadius="8" Padding="20" BorderBrush="{DynamicResource BordaCard}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.15" ShadowDepth="2" Direction="270"/>
        </Border.Effect>
        
        <StackPanel VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock Text="🔓" FontSize="26" Margin="0,0,10,0"/>
                <TextBlock Text="Liberação de Desconto" FontSize="22" FontWeight="Bold" Foreground="#D32F2F" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock FontSize="14" Foreground="{DynamicResource TextMuted}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20"
           Text="Insira as credenciais de um superior para elevar o limite de desconto desta venda."/>

            <TextBlock Text="Selecione o Gerente:" FontWeight="SemiBold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
            <ComboBox Name="CmbGerentes" DisplayMemberPath="Name" Padding="5" Margin="0,0,0,15" FontSize="14"/>

            <TextBlock Text="Senha de Autorização:" FontWeight="SemiBold" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
            <PasswordBox Name="TxtSenhaGerente" Padding="5" Margin="0,0,0,20" FontSize="14"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="CANCELAR" Height="40" Background="{DynamicResource CardBgAlt}" Foreground="{DynamicResource TextMuted}" FontWeight="Bold" Cursor="Hand" Click="BtnCancelar_Click">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
                
                <Button Grid.Column="2" Content="AUTORIZAR" Height="40" Background="#185A9D" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="BtnAutorizar_Click">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
            </Grid>
        </StackPanel>
    </Border>
</Window>