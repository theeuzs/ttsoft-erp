<Window x:Class="ERP.WPF.Views.SalesReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Relatório de Vendas - TTSoft" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" Background="{DynamicResource FundoGeral}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource CardBg}" CornerRadius="8" Padding="15" Margin="0,0,0,20" BorderBrush="{DynamicResource BordaCard}" BorderThickness="1">
            <WrapPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Data Inicial:" FontWeight="SemiBold" Foreground="{DynamicResource TextDark}" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding StartDate}" Width="120" Height="30"/>
                </StackPanel>
                
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Data Final:" FontWeight="SemiBold" Foreground="{DynamicResource TextDark}" Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding EndDate}" Width="120" Height="30"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Vendedor:" FontWeight="SemiBold" Foreground="{DynamicResource TextDark}" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding AvailableSellers}" SelectedItem="{Binding SelectedSeller}" Width="150" Height="30" Foreground="{DynamicResource TextDark}" Background="{DynamicResource FundoInput}"/>
                </StackPanel>

                <Button Content="🔍 BUSCAR" Command="{Binding SearchCommand}" 
                        Width="100" Height="30" Margin="0,0,10,0" VerticalAlignment="Bottom"
                        Background="#3B82F6" Foreground="White" FontWeight="Bold" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="📄 Exportar PDF" Command="{Binding ExportarPdfCommand}"
                        Background="#DC2626" Foreground="White" Width="120" Height="30" 
                        VerticalAlignment="Bottom" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style>
                    </Button.Resources>
                </Button>
            </WrapPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource CardBg}" CornerRadius="8" Padding="20" BorderThickness="1" BorderBrush="{DynamicResource BordaCard}">
                <StackPanel>
                    <TextBlock Text="Faturamento Total" FontSize="14" Foreground="{DynamicResource TextMuted}"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=C}" FontSize="28" FontWeight="Bold" Foreground="#F59E0B" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource CardBg}" CornerRadius="8" Padding="20" BorderThickness="1" BorderBrush="{DynamicResource BordaCard}">
                <StackPanel>
                    <TextBlock Text="Quantidade de Vendas" FontSize="14" Foreground="{DynamicResource TextMuted}"/>
                    <TextBlock Text="{Binding TotalSalesCount}" FontSize="28" FontWeight="Bold" Foreground="#10B981" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Sales}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                  Background="Transparent" BorderThickness="1" BorderBrush="{DynamicResource BordaCard}" RowHeight="35" AlternatingRowBackground="{DynamicResource CardBgAlt}">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource CardBg}"/>
                    <Setter Property="Foreground" Value="{DynamicResource CorAcento}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BordaCard}"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="DATA" Binding="{Binding DataVenda, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                <DataGridTextColumn Header="RECIBO" Binding="{Binding NumeroRecibo}" Width="150"/>
                <DataGridTextColumn Header="CLIENTE" Binding="{Binding ClienteNome}" Width="*"/>
                <DataGridTextColumn Header="VENDEDOR" Binding="{Binding VendedorNome}" Width="150"/>
                <DataGridTextColumn Header="PAGAMENTO" Binding="{Binding FormaPagamento}" Width="120"/>
                <DataGridTextColumn Header="TOTAL" Binding="{Binding ValorTotal, StringFormat=C}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#10B981"/>
                            <Setter Property="Margin" Value="5,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>