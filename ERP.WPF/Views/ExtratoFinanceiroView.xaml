<UserControl x:Class="ERP.WPF.Views.ExtratoFinanceiroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource CardBgAlt}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="EXTRATO FINANCEIRO" FontSize="24" FontWeight="Bold"
                   Foreground="{DynamicResource TextDark}" Margin="0,0,0,20"/>

        <!-- Filtro de período -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="140"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="De" FontWeight="SemiBold" FontSize="11"/>
                <DatePicker SelectedDate="{Binding DataInicio}" Height="34"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Até" FontWeight="SemiBold" FontSize="11"/>
                <DatePicker SelectedDate="{Binding DataFim}" Height="34"/>
            </StackPanel>

            <Button Grid.Column="4" Content="BUSCAR" Command="{Binding BuscarCommand}"
                    Background="#3B82F6" Foreground="White" FontWeight="Bold" Height="34"
                    VerticalAlignment="Bottom" Cursor="Hand"/>
        </Grid>

        <!-- Resumo -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource CardBg}" Padding="14" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="ENTRADAS" FontSize="11" FontWeight="SemiBold" Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalEntradas, StringFormat=C2}" FontSize="20" FontWeight="Bold" Foreground="#16A34A"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource CardBg}" Padding="14" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="SAÍDAS" FontSize="11" FontWeight="SemiBold" Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalSaidas, StringFormat=C2}" FontSize="20" FontWeight="Bold" Foreground="#DC2626"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="{DynamicResource CardBg}" Padding="14" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="PENDENTE (CARTÃO NO LIMBO)" FontSize="11" FontWeight="SemiBold" Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalPendente, StringFormat=C2}" FontSize="20" FontWeight="Bold" Foreground="#F59E0B"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Timeline -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Itens}" AutoGenerateColumns="False"
                  Background="{DynamicResource CardBg}" GridLinesVisibility="Horizontal" RowHeight="36">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding DataHora, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>
                <DataGridTextColumn Header="Origem" Binding="{Binding Origem}" Width="180"/>
                <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Tipo}" Value="Entrada">
                                    <Setter Property="Foreground" Value="#16A34A"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tipo}" Value="Saída">
                                    <Setter Property="Foreground" Value="#DC2626"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tipo}" Value="Recebível Pendente">
                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tipo}" Value="Recebível Liquidado">
                                    <Setter Property="Foreground" Value="#94A3B8"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C2}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Tipo}" Value="Entrada">
                                    <Setter Property="Foreground" Value="#16A34A"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tipo}" Value="Saída">
                                    <Setter Property="Foreground" Value="#DC2626"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tipo}" Value="Recebível Pendente">
                                    <Setter Property="Foreground" Value="#F59E0B"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Tipo}" Value="Recebível Liquidado">
                                    <Setter Property="Foreground" Value="#94A3B8"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
