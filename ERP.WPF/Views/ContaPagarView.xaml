<UserControl x:Class="ERP.WPF.Views.ContaPagarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource CardBgAlt}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CONTAS A PAGAR (DESPESAS)" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextDark}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/> <ColumnDefinition Width="20"/>  <ColumnDefinition Width="*"/>   </Grid.ColumnDefinitions>

            <Border Background="{DynamicResource CardBg}" Padding="20" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="NOVO LANÇAMENTO" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="Descrição (Ex: Conta de Luz)" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Descricao, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Height="30" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Valor (R$)" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Valor, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Height="30" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Categoria (Ex: Fixo, Fornecedor)" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Categoria, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Height="30" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Vencimento" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding DataVencimento}" Margin="0,5,0,25" Height="30"/>

                    <Button Content="LANÇAR DESPESA" Command="{Binding AdicionarCommand}" Background="#3B82F6" Foreground="White" FontWeight="Bold" Height="40">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>

            <DataGrid Grid.Column="2" ItemsSource="{Binding Contas}" AutoGenerateColumns="False" IsReadOnly="True" Background="{DynamicResource CardBg}" RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*"/>
                    <DataGridTextColumn Header="Categoria" Binding="{Binding Categoria}" Width="120"/>
                    <DataGridTextColumn Header="Vencimento" Binding="{Binding DataVencimento, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C2}" Width="100"/>
                    
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="Pendente">
                                        <Setter Property="Foreground" Value="#EF4444"/> </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Pago">
                                        <Setter Property="Foreground" Value="#22C55E"/> </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Width="150" Header="Ações">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="PAGAR" Command="{Binding DataContext.PagarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#22C55E" Foreground="White" Margin="0,0,5,0" Padding="10,5" Cursor="Hand"/>
                                    <Button Content="EXCLUIR" Command="{Binding DataContext.ExcluirCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Background="#EF4444" Foreground="White" Padding="10,5" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>