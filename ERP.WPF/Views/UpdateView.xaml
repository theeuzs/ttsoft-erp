<Window x:Class="ERP.WPF.Views.UpdateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Atualização Disponível" Height="260" Width="480"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Border Background="{DynamicResource CardBg}" CornerRadius="14" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.2" ShadowDepth="4"/>
        </Border.Effect>
        <StackPanel Margin="32,28">
            <TextBlock Text="🚀 Nova versão disponível!"
                       FontSize="18" FontWeight="Bold" Foreground="#166534"/>
            <TextBlock x:Name="TxtVersao" Margin="0,8,0,0"
                       FontSize="13" Foreground="#374151"/>
            <TextBlock x:Name="TxtNotas" Margin="0,4,0,16"
                       FontSize="12" Foreground="#6B7280" TextWrapping="Wrap"/>

            <ProgressBar x:Name="PgbDownload" Height="8" Minimum="0" Maximum="100"
                         Foreground="#166534" Background="#F0FDF4"
                         Visibility="Collapsed" Margin="0,0,0,12"/>
            <TextBlock x:Name="TxtProgresso" FontSize="11"
                       Foreground="#6B7280" HorizontalAlignment="Center"
                       Visibility="Collapsed" Margin="0,0,0,12"/>

            <StackPanel x:Name="PnlBotoes" Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="BtnDepois" Content="Depois" Click="BtnDepois_Click"
                        Width="90" Height="34" Margin="0,0,10,0"
                        Background="{DynamicResource CardBgAlt}" Foreground="{DynamicResource TextMuted}"
                        BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="BtnAtualizar" Content="✅ Atualizar Agora"
                        Click="BtnAtualizar_Click"
                        Width="140" Height="34"
                        Background="#166534" Foreground="White"
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>