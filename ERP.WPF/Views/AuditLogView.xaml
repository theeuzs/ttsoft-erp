<UserControl x:Class="ERP.WPF.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource FundoRodape}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Auditoria de Sistema" FontSize="28" FontWeight="Black" Foreground="#0F172A"/>
            <TextBlock Text="Rastreamento completo de ações, alterações e exclusões." FontSize="14" Foreground="{DynamicResource TextMuted}"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{DynamicResource CardBg}" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,15,0">
                    <TextBlock Text="De:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource TextMuted}" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding DataInicio}" Width="120"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,15,0">
                    <TextBlock Text="Até:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource TextMuted}" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding DataFim}" Width="120"/>
                </StackPanel>

                <TextBox Grid.Column="2" Text="{Binding Busca, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="10,5" Margin="0,0,15,0" FontSize="14"
                         ToolTip="Busque por Operador, Ação ou Tabela"/>

                <Button Grid.Column="3" Content="🔍 FILTRAR" Command="{Binding FiltrarCommand}" 
                        Background="#0EA5E9" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource CardBg}" CornerRadius="8" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" IsReadOnly="True" 
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0"
                      Background="Transparent" MinRowHeight="40" RowBackground="{DynamicResource CardBg}" AlternatingRowBackground="{DynamicResource CardBgAlt}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data e Hora" Binding="{Binding Timestamp, StringFormat='dd/MM/yyyy HH:mm:ss'}" Width="140" Foreground="{DynamicResource TextMuted}"/>
                    <DataGridTextColumn Header="Usuário" Binding="{Binding UserName}" Width="150" FontWeight="Bold" Foreground="#0F172A"/>
                    <DataGridTextColumn Header="Ação" Binding="{Binding Action}" Width="80" Foreground="#0EA5E9" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Tabela" Binding="{Binding EntityType}" Width="120" Foreground="{DynamicResource TextMuted}"/>
                    <DataGridTextColumn Header="Registro Alterado" Binding="{Binding EntityId}" Width="180" Foreground="#0F172A" FontWeight="SemiBold"/>
                    
                    <DataGridTemplateColumn Header="Alterações" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ResumoAlteracoes, Mode=OneWay}"
                                           TextWrapping="Wrap" Padding="6,4"
                                           MaxWidth="700"
                                           Foreground="{DynamicResource TextDark}"
                                           VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Action}" Value="INSERT">
                                                    <Setter Property="Foreground" Value="#10B981"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Action}" Value="DELETE">
                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Action}" Value="UPDATE">
                                                    <Setter Property="Foreground" Value="#0F172A"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>