<Window x:Class="ERP.WPF.Views.TrocarSenhaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Troca de Senha Obrigatória"
        Width="380" Height="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🔒 Troca de senha obrigatória"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="Por segurança, você precisa definir uma nova senha antes de continuar."
                   FontSize="12" Foreground="#64748B" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Text="SENHA ATUAL" Style="{StaticResource LabelStyle}"/>
        <PasswordBox Grid.Row="3" x:Name="PbSenhaAtual" PasswordChanged="PbSenhaAtual_PasswordChanged"/>

        <TextBlock Grid.Row="4" Text="NOVA SENHA (mín. 8 caracteres)" Style="{StaticResource LabelStyle}"/>
        <PasswordBox Grid.Row="5" x:Name="PbNovaSenha" PasswordChanged="PbNovaSenha_PasswordChanged"/>

        <StackPanel Grid.Row="6" VerticalAlignment="Top">
            <TextBlock Text="CONFIRMAR NOVA SENHA" Style="{StaticResource LabelStyle}"/>
            <PasswordBox x:Name="PbConfirmar" PasswordChanged="PbConfirmar_PasswordChanged"/>

            <TextBlock Text="{Binding MensagemErro}"
                       Foreground="#EF4444" FontSize="12" TextWrapping="Wrap"
                       Visibility="{Binding TemErro, Converter={StaticResource BoolToVis}}"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <Button Grid.Row="7" Content="CONFIRMAR" Command="{Binding ConfirmarCommand}"
                Height="40" Margin="0,16,0,0" Background="#1E62A6" Foreground="White"
                FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
    </Grid>
</Window>
